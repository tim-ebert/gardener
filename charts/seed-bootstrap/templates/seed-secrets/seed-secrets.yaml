{{ range $name, $secret := .Values.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name | quote }}
  namespace: {{ $.Release.Namespace }}
type: {{ $secret.type }}
data:
  {{- toYaml $secret.data | nindent 2 }}
{{- end }}
