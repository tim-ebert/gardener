apiVersion: v1
kind: Service
metadata:
  name: vpa-webhook
  namespace: {{ .Release.Namespace }}
  labels:
    {{- toYaml .Values.labels | nindent 4 }}
spec:
  ports:
    - port: 443
      targetPort: 443
  selector:
    app: vpa-admission-controller
